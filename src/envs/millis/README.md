# Millis Environment

## Purpose

Tests the millisecond timer interrupt by incrementing the LED counter once per second.

## Hardware Requirements

- 3-LED binary counter connected to PC0–PC2

## Expected Outcome

The LED counter increments at a steady 1 Hz rate (once every 1000 ms), confirming that the timer interrupt is firing correctly.
