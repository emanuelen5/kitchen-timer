# Native Environment

## Purpose

A dummy environment used to verify that the project compiles successfully on a non-AVR (native) host. It contains no hardware-specific code and is mainly used as a build sanity check.

## Hardware Requirements

None — runs on the host machine.

## Expected Outcome

The program compiles and exits with code 0.
