# Unit-testing on the native host

This sub-directory contains tests that can run on the native host, without any hardware connected.

## Run the tests

To run the tests, run the following.

```bash
pio test -e native
```
